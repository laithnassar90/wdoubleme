/**
 * DEPRECATED — the figma/ import folder has been consolidated.
 * ImageWithFallback now lives at src/components/ImageWithFallback.tsx
 *
 * This barrel keeps any existing imports from breaking:
 *   import { ImageWithFallback } from '../components/figma';  // still works
 */
export { ImageWithFallback } from '../ImageWithFallback';
