# ── Build output ──────────────────────────────────────────────
build/
dist/
.vite/

# ── Dependencies ───────────────────────────────────────────────
node_modules/

# ── Environment variables (NEVER commit these) ─────────────────
.env
.env.local
.env.development
.env.development.local
.env.test
.env.test.local
.env.production
.env.production.local

# ── Test output ────────────────────────────────────────────────
coverage/
test-results/
playwright-report/

# ── OS junk ────────────────────────────────────────────────────
.DS_Store
Thumbs.db
desktop.ini

# ── Logs & dev server output ───────────────────────────────────
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
vite-out.log
vite-err.log

# ── Editor artifacts ───────────────────────────────────────────
.vscode/settings.json
.idea/
*.suo
*.user

# ── Firebase / Google native config (not needed for web) ───────
src/GoogleService-Info.plist
src/google-services.json
src/imports/google-services.json

# ── QA / CI artefacts committed accidentally to root ───────────
# Lighthouse and Axe audit JSON dumps
landing-axe.json
landing-desktop.json
landing-desktop-fold.json
landing-desktop.png.json
landing-mobile.json
landing-mobile-fold.json
# Screenshot PNGs generated by CI / Playwright
landing-desktop.png
landing-desktop-fold.png
landing-mobile.png
landing-mobile-fold.png
# Any future Lighthouse / axe JSON or PNG captures at repo root
*-axe.json
*-desktop*.json
*-mobile*.json
*-desktop*.png
*-mobile*.png

# ── Deprecated vitest config (replaced by vitest.config.ts) ───
vitest.config.mjs

# ── Cleanup backup (safe to delete after review) ───────────────
_REMOVED/

# Local Codex worktrees / ad hoc artifacts
.codex-landing-deploy/
mobility-os-current.png
preview-log.txt
